# Plan 7: End-to-End Demo & Integration Test

**Goal:** Prove the language works for its target use case by building a comprehensive multi-module program that exercises all features together.

**What the demo exercises:**
- Multi-module imports with cross-module function calls
- Leveled logging (log_set_level, log_info)
- JSON (build objects/arrays, stringify, parse, access fields)
- String operations (concat, substring, len, int_to_string, string_to_int)
- File I/O (file_write, file_append, file_exists, file_read)
- If/else control flow
- While loops (in math module)
- Integer arithmetic
- Exit code as return value

**Files:**
- `tests/fixtures/demo_backend/main.sans` — main program
- `tests/fixtures/demo_backend/math.sans` — math utility module
- `examples/hello_sans.sans` — standalone hello world example

**Test:** `e2e_demo_backend` — compiles and runs the multi-module demo, verifies exit code = 30

**All 293 tests pass.**
