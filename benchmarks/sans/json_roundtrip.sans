main() {
  i := 0
  result := 0
  while i < 100 {
    j = jo()
    k := 0
    while k < 1000 {
      j.set(str(k), ji(k))
      k += 1
    }
    s = jfy(j)
    parsed = jp(s)
    result = parsed.get("999").get_int()
    i += 1
  }
  p(result)
  0
}
