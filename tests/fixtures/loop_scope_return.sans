// Test early return from inside a loop with loop scope
find_len(target:I) I {
  i := 0
  while i < 10 {
    s = "item_" + str(i)
    if i == target { return slen(s) }
    i += 1
  }
  0
}

main() I {
  find_len(3)
}
