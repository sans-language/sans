trait Ops {
  fn first(self) Int
  fn second(self) Int
}

struct Data { a Int, b Int }

impl Ops for Data {
  fn first(self) Int { self.a }
  fn second(self) Int { self.b }
}

fn use_ops(d dyn Ops) Int {
  d.first() + d.second()
}

main() I {
  d = Data { a: 10, b: 32 }
  use_ops(d as dyn Ops)
}
