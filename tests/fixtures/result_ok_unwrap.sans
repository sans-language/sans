divide(a:I b:I) R<I> {
    if b == 0 {
        err("division by zero")
    } else {
        ok(a / b)
    }
}

main() {
    r1 = divide(10 2)
    r2 = divide(20 4)
    v1 = r1!
    v2 = r2!
    v1 + v2
}
